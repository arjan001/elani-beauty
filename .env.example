# ==========================================
# CLASSY COLLECTIONS - ENVIRONMENT VARIABLES
# ==========================================
# Copy this file to .env.local for development
# Add these variables to Netlify in Site Settings > Build & Deploy > Environment

# ==========================================
# SUPABASE CONFIGURATION (REQUIRED)
# ==========================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Public Supabase URL (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (safe to expose, limited permissions)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (KEEP SECRET - admin access)
# Only needed for server-side operations and admin routes
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Direct Supabase URL (for backups)
SUPABASE_URL=https://your-project.supabase.co

# ==========================================
# DATABASE CONFIGURATION (Optional - if using Prisma)
# ==========================================
# Connection strings for direct database access

# Standard PostgreSQL connection URL
POSTGRES_URL=postgresql://postgres:password@your-host:5432/your_db

# Prisma-optimized connection URL (with connection pooling)
POSTGRES_PRISMA_URL=postgresql://postgres:password@your-host:5432/your_db

# Non-pooled connection URL (for migrations)
POSTGRES_URL_NON_POOLING=postgresql://postgres:password@your-host:5432/your_db

# Database credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DATABASE=your_db_name
POSTGRES_HOST=your-project.supabase.co

# ==========================================
# AUTHENTICATION (REQUIRED)
# ==========================================

# Supabase JWT Secret (for token verification)
SUPABASE_JWT_SECRET=your_jwt_secret_here

# Supabase Anon Key (alternative name, same as above)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ==========================================
# EMAIL CONFIGURATION (Optional - for transactional emails)
# ==========================================
# Configure SMTP for order confirmations and notifications
# Supported: Gmail, SendGrid, AWS SES, custom SMTP

# SMTP Server Host
SMTP_HOST=smtp.gmail.com

# SMTP Server Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP Authentication User (usually your email)
SMTP_USER=your-email@gmail.com

# SMTP Authentication Password (app-specific password for Gmail)
SMTP_PASS=your_app_password_here

# From Address Name (displays in email)
SMTP_FROM_NAME=Classy Collections

# From Email Address (email sender address)
SMTP_FROM_EMAIL=noreply@classycollections.com

# ==========================================
# PAYMENT INTEGRATION - M-PESA (Optional)
# ==========================================
# Get these from: Daraja API (M-Pesa Developer Portal)

# M-Pesa Consumer Key
MPESA_CONSUMER_KEY=your_consumer_key

# M-Pesa Consumer Secret
MPESA_CONSUMER_SECRET=your_consumer_secret

# M-Pesa Business Shortcode
MPESA_SHORTCODE=174379

# M-Pesa Passkey (from Daraja Portal)
MPESA_PASSKEY=your_passkey_here

# M-Pesa Callback URL (where payment confirmations are sent)
MPESA_CALLBACK_URL=https://your-domain.com/api/mpesa/callback

# ==========================================
# STORAGE CONFIGURATION (Optional - Vercel Blob)
# ==========================================
# For file uploads and image hosting

# Vercel Blob Token (from Vercel Project Settings)
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# ==========================================
# ANALYTICS & TRACKING (Optional)
# ==========================================

# Google Analytics Measurement ID
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Facebook Pixel ID
FACEBOOK_PIXEL_ID=123456789

# ==========================================
# APPLICATION CONFIGURATION
# ==========================================

# Environment (production, development, staging)
NODE_ENV=production

# Next.js Configuration
NEXT_PUBLIC_APP_URL=https://classycollections.com

# API Gateway (if using Vercel AI Gateway or similar)
AI_GATEWAY_API_KEY=your_ai_gateway_key

# ==========================================
# SOCIAL MEDIA & CONTACT
# ==========================================

# WhatsApp Business Phone Number
NEXT_PUBLIC_WHATSAPP_PHONE=254713809695

# Instagram Handle
NEXT_PUBLIC_INSTAGRAM_HANDLE=_classycollections

# TikTok Handle
NEXT_PUBLIC_TIKTOK_HANDLE=_classycollections

# Twitter/X Handle
NEXT_PUBLIC_TWITTER_HANDLE=_classycollections

# Business Email
NEXT_PUBLIC_CONTACT_EMAIL=info@classycollections.com

# ==========================================
# NETLIFY DEPLOYMENT CONFIGURATION
# ==========================================
# Add these to: Site Settings > Build & Deploy > Environment variables

# Build command (if needed)
# npm run build

# Publish directory
# out

# Framework Detection
# nextjs

# ==========================================
# SECURITY NOTES
# ==========================================
# 1. NEVER commit .env.local to Git
# 2. Use different keys for development and production
# 3. Keep SUPABASE_SERVICE_ROLE_KEY secret - NEVER expose in frontend
# 4. Rotate SMTP passwords regularly
# 5. Use strong, unique passwords for all services
# 6. Enable 2FA on all external service accounts
# 7. Review Netlify environment variables periodically

# ==========================================
# HOW TO SET UP ON NETLIFY
# ==========================================
# 1. Go to Netlify Site Settings
# 2. Select "Build & Deploy" > "Environment"
# 3. Click "Edit variables"
# 4. Add each variable from this file (mark as sensitive where indicated)
# 5. Redeploy: "Trigger deploy" > "Deploy site"

# Variables marked SENSITIVE in Netlify UI:
# - SUPABASE_SERVICE_ROLE_KEY
# - POSTGRES_PASSWORD
# - SMTP_PASS
# - MPESA_CONSUMER_SECRET
# - BLOB_READ_WRITE_TOKEN
# - All API keys and tokens

# ==========================================
# QUICK SETUP CHECKLIST
# ==========================================
# [ ] Copy this file to .env.local (development)
# [ ] Fill in Supabase URL and Keys
# [ ] Set up SMTP if sending emails
# [ ] Configure M-Pesa if accepting payments
# [ ] Test locally: npm run dev
# [ ] Add to Netlify environment variables
# [ ] Redeploy on Netlify
# [ ] Test in production
